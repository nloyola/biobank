all:
  children:
    biobank:
      hosts:
        biobankarchive:
          # Per-site: the VM's address and the sudo-capable SSH user.
          ansible_host: biobankarchive.lunenfeld.ca
          ansible_user: bbadmin
