all:
  children:
    biobank:
      hosts:
        biobank-site:
          # Per-site: set to the new VM's address and the sudo-capable SSH user.
          ansible_host: 192.168.50.13
          ansible_user: biobank
